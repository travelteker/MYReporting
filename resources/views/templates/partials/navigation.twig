
    <nav class="navbar navbar-inverse navbar-fixed-top"> 
        <div class="container"> 
            <div class="navbar-header"> 
                <button type="button" class="collapsed navbar-toggle" data-toggle="collapse" data-target="#all-menus" aria-expanded="false"> 
                    <span class="sr-only">Toggle navigation</span> 
                    <span class="icon-bar"></span> 
                    <span class="icon-bar"></span> 
                    <span class="icon-bar"></span> 
                </button> 
                <a href="{{ path_for('home') }}" class="navbar-brand"><i class="fas fa-globe"></i> MYReporting</a> 
            </div> 
            <div class="collapse navbar-collapse" id="all-menus"> 
                {% if auth.check %}
                <ul class="nav navbar-nav">
                    
                    <li {% if home == 'active' %} class="active" {% endif %}>
                        <a href="{{ path_for('home') }}">Home</a>
                    </li>
                   
                    <li {% if reports == 'active' %} class="active" {% endif %}>
                        <a href="{{ path_for('reports') }}">Reports</a>
                    </li>
                </ul>
                {% endif %}
                <ul class="nav navbar-nav navbar-right">
                    {% if auth.check %}
                        <li class="dropdown">
                            <a href="#" class="dropdonw-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                            <i class="fas fa-user-cog"></i> {{ auth.user.name }}
                            <span class="caret"><span>
                            </a>
                            <ul class="dropdown-menu">
                                <li><a href="{{ path_for('auth.password.change') }}"><i class="fas fa-exchange-alt"></i> Change Password</a></li>
                                <li><a href="{{ path_for('auth.signout') }}"><i class="fas fa-sign-out-alt"></i> Sing Out</a></li>
                            </ul>
                        </li>
                    {% else %}
                        <li><a href="{{ path_for('auth.signup') }}"><i class="fas fa-address-card"></i>  Sing Up</a></li>
                        <li><a href="{{ path_for('auth.signin') }}"><i class="fas fa-lock"></i>  Sing In</a></li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav> 
